# [[315,66,7]] by check deletion from 315-65-9

## Hypothesis

Every stabiliser generator costs one logical qubit, so deleting a single row of
H_Z returns a code with k + 1 logical qubits on the same n physical qubits at
a distance that can only fall or stay put.

## Which row matters

The same parent, `codes/315-65-9.json`, gives [[315,66,5]] when row 0 of H_X is
deleted and [[315,66,7]] when row 63 of H_Z is deleted. Same n, same k, same
check weight, two more distance points. The choice of row is not incidental:
sweeping every row of both sides and keeping the best is what separates the two,
and the weaker one is dominated outright by the stronger.

## Construction

Parent: the board entry `codes/315-65-9.json`. Row 63 of H_Z is removed and
every other check is untouched.

## Distance

Two-sided random information-set search first. That search returns the minimum
over both sides, so when one side is much lighter the other never surfaces even
though the schema needs a witness for it. The missing side is then searched on
its own in the quotient ker(H_opposite) / rowspace(H_own), by randomly permuting
and reducing a kernel basis and keeping the lightest row that stays outside the
row space.

Deleting a row also leaves H_X and H_Z with different shapes, so the argument
order of a two-sided search stops being a reliable side label; every witness here
is classified by the conditions it actually satisfies.

## Boundary

Distance is an upper bound from witness search, not a proof. The parent's own
distance claim is inherited context, not re-derived. Literature novelty is
unverified.
