--- a/src/solver/Solver.h
+++ b/src/solver/Solver.h
@@ -550,6 +550,7 @@
     uint64_t objForSearch, fixedCostBySearch, derivedCost;
     void cancelUntilBeginning(int begnning);
     void checkSolution();
+    virtual void observeIncumbent(uint64_t cost) {}
     void noteBestSolution(uint64_t unsatSoft);
     void emitBoundsUpdate();
     void emitTryUpdate(uint64_t try_val);
--- a/src/solver/Solver.cc
+++ b/src/solver/Solver.cc
@@ -5224,6 +5224,7 @@
     if (!bestSolutionFound || unsatSoft < bestSup) {
         bestSolutionFound = true;
         bestSup = unsatSoft;
+        observeIncumbent(getCostUB());
         emitBoundsUpdate();
     }
 }
--- a/src/core/distqldpc.cc
+++ b/src/core/distqldpc.cc
@@ -38,6 +38,9 @@
 
 #include "utils/System.h"
 #include "SimpSolver.h"
+#include "observer.h"
+
+static const char* benchmark_witness_file = NULL;
 
 using namespace Minisat;
 
@@ -168,14 +171,14 @@
 
 static int min_distance_stabilizer_maxsat(
     const Matrix& Hx, const Matrix& Hz, const Matrix& Gx, const Matrix& Gz,
-    int cpu_lim, int verb, int bounds_pipe_w, int card_mode)
+    double cpu_lim, int verb, int bounds_pipe_w, int card_mode)
 {
     const int n = Hx.cols;
     if (Hz.cols != n || Gx.cols != n || Gz.cols != n) die("matrix column mismatch");
     const int k_log = Gx.rows + Gz.rows;
     if (k_log == 0) return INT_MAX;
 
-    SimpSolver S;
+    ObservedSimpSolver S(benchmark_witness_file, n);
     S.setBoundsPipe(bounds_pipe_w);
     S.cardinalityEncMode = card_mode;
     S.parsing = true;
@@ -424,7 +427,7 @@
 
 static int solve_in_child_fork(
     const Matrix& Hx, const Matrix& Hz, const Matrix& Gx, const Matrix& Gz,
-    int cpu_lim, int verb, int card_mode, BoundsBook& out, bool& timed_out,
+    double cpu_lim, int verb, int card_mode, BoundsBook& out, bool& timed_out,
     ProgressState& prog)
 {
     int pipefd[2];
@@ -471,6 +474,8 @@
         int status = 0;
         pid_t w = waitpid(pid, &status, WNOHANG);
         if (w == pid) {
+            if (!WIFEXITED(status) || WEXITSTATUS(status) > 1)
+                die("solver child failed (including witness export failure)");
             drain_bounds_pipe(pipefd[0], book, carry, &prog);
             if (!carry.empty())
                 apply_pipe_line(book, carry.c_str(), &prog);
@@ -505,13 +510,15 @@
 
 int main(int argc, char** argv) {
     const char* prefix = NULL;
-    int cpu_lim = INT32_MAX;
+    double cpu_lim = INT32_MAX;
     int verb = 0;
     int card_mode = 1;  /* CARD_ENC_BOTH */
 
     for (int i = 1; i < argc; i++) {
         if (!strncmp(argv[i], "-cpu-lim=", 9))
-            cpu_lim = atoi(argv[i] + 9);
+            cpu_lim = strtod(argv[i] + 9, NULL);
+        else if (!strncmp(argv[i], "-witness-file=", 14))
+            benchmark_witness_file = argv[i] + 14;
         else if (!strcmp(argv[i], "-v") || !strcmp(argv[i], "-debug"))
             verb = 1;
         else if (!strcmp(argv[i], "-q"))
@@ -543,6 +550,7 @@
             die("unexpected argument");
     }
     if (!prefix) die("need code name (e.g. AJ_01)");
+    if (!(cpu_lim > 0 && cpu_lim <= INT32_MAX)) die("invalid timeout");
     if (card_mode != 0 && card_mode != 1 && card_mode != 2 && card_mode != 3 && card_mode != 4)
         die("invalid cardinality mode");
 
